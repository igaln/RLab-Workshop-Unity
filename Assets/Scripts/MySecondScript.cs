using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MySecondScript : MyFirstScript
{
    // Start is called before the first frame update
    void Start()
    {
		Debug.Log(UserName);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
